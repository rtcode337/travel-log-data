# travel-log-data

[travel-log](https://github.com/rtcode337/travel-log)本体には同梱しない、容量の大きいスポットの
初期データ(シード用CSV)を置くリポジトリ。travel-log本体とは独立して更新・管理する。

> **このリポジトリ全体に適用される単一のライセンスはありません。** フォルダ(スポットキー)
> ごとに由来元のライセンス・利用制限が異なるため、リポジトリ直下に`LICENSE`は置いていません。
> 再配布・二次利用の際は、必ず「データの出典・ライセンス表示」の節を確認してください。

## フォルダ構成

`<スポットキー>/`(`spot_types.key`、例: `post_office`)フォルダの下に、そのスポット種別の
データを置く。

```
catalog.json                         # スポット種別の一覧(key・label)。travel-log側の「GitHubリポジトリからスポット種別取り込み」が参照
tourist/
  spots.csv
  settings.json
  excluded_candidates/
    01_hokkaido.md 〜 47_okinawa.md  # 除外した候補と理由(都道府県別、参考資料)
    series_demotions.md              # シリーズを目視で格下げしたスポットの記録
    exclude.txt                      # travel-log側から削除するスポットのkey一覧(追記式)
post_office/
  spots.csv
  settings.json
goshuin/
  spots.csv
  settings.json
buratamori/
  spots.csv
  routes.csv
  settings.json
  excluded_candidates/
    exclude.txt                      # travel-log側から削除するスポットのkey一覧(追記式)
suiyou_dodesho_domestic/
  spots.csv
  routes.csv
  settings.json
  excluded_candidates/
    exclude.txt
suiyou_dodesho_overseas/
  spots.csv
  routes.csv
  settings.json
restaurant/
  spots.csv
  settings.json
  excluded_candidates/
    ramen.md                         # 収録しなかった候補と理由(ラーメン、参考資料)
    other_genres.md                  # 収録しなかった候補と理由(ラーメン以外、参考資料)
    osm_wikipedia_sweep.md           # OSM側のwikipediaタグ起点で洗い直した記録(参考資料)
    exclude.txt                      # travel-log側から削除するスポットのkey一覧(追記式)
```

リポジトリ直下の`catalog.json`は`{ "spot_types": [ { "key": "...", "label": "..." }, ... ] }`形式の
スポット種別カタログで、travel-log側の管理画面「GitHubリポジトリからスポット種別取り込み」が種別の一覧表示に
使う(選んだ種別の`settings.json`・`spots.csv`・`excluded_candidates/exclude.txt`・`routes.csv`が
一括適用される)。**スポットキーのフォルダを追加・改名したら`catalog.json`にも反映すること**。

`tourist/`(観光地)はtravel-log側の唯一の既定スポット種別(`spot_types`の行はアプリ初期化時に
自動で作られる)だが、スポットデータ自体は他のスポットキーと同様、ここに置くCSVがそのまま
取り込み用の参照データであり、travel-log本体側に複製は持たない。`settings.json`も
travel-log側では`db/init/01_schema.sql`が直接種別を作成するため実際には使われず、
現状の設定を文書化するためだけに置いてある(かつては`db/init/tourist_spots.csv`として
travel-log本体にも複製し自動投入していたが、下記「データの出典・ライセンス」の理由により
廃止した)。

スポットデータはCSV、スポットを巡った順に矢印で繋ぐルートは`routes.csv`、
スポット種別そのものの初期設定は`settings.json`で持つ。

### スポットデータ(CSV)

CSV形式はtravel-log本体の`/[type]/admin`のCSVインポート機能
(`components/AdminView.tsx`の`CSV_COLUMNS`)に合わせること。

```csv
name,name_kana,lat,lng,region,series,categories,description,key
```

- 必須列: `name`, `lat`, `lng`, `region`, `key`(`key`はこのリポジトリの運用上の必須。
  travel-log側のCSV取り込みでは省略可)
- `region`はスポット種別の`region_scope`設定に応じた地域(既定`'jp'`=都道府県、
  国コード指定=州・県、`'world'`=国名)
- `series`/`categories`はスポット種別ごとに意味が異なってよい自由入力(空でも可)
- `key`は省略可の種別内一意な参照キーで、`routes.csv`がスポットを指すのに使う
  (ルートを持たない種別では列ごと省略してよい)。一度割り当てたら変更しない
- 取り込みはtravel-log側の管理画面(`/[type]/admin`)からこのCSVファイルを
  手動アップロードして行う(自動取り込みの仕組みは今のところ無い)

### ルートデータ(routes.csv)

スポットを巡った順に矢印で繋ぐルートの定義(列:
`route,series,seq,spot_key,description,leg_description`)。
取り込むとtravel-log側の地図に、経由地を`seq`昇順に繋いだラインと進行方向の矢印が描かれる。
`series`列に`settings.json`の`series`のシリーズ値を入れると
矢印がそのシリーズの縁取り色で描かれ、地図のシリーズ絞り込みにも連動する
(`route`はルートの表示名で、シリーズとは独立)。`description`列はルート全体の説明文で、
地図でルートの線をタップすると出るルート詳細の先頭に表示される。`leg_description`列は
その行のスポットから次のスポットへの区間の説明(移動手段など。行単位で、最終地点の行は
空欄にする)で、ルート詳細の経由地一覧で2点の間に表示される。`spot_key`は
スポットCSVの`key`列を指すため、**スポットCSV→routes.csvの順で**同じ管理画面から
取り込む。スキーマの詳細はCLAUDE.mdの「routes.csv形式」節を参照

### スポット種別の設定(settings.json)

`spot_types`(key・表示名)と`spot_type_settings`(公開範囲・口コミ・Wikipediaリンクなどの
ON/OFF設定、シリーズ・カテゴリの一覧)をまとめて1つのスポット種別として作成するための定義ファイル。

```json
{
  "key": "post_office",
  "label": "郵便局",
  "settings": {
    "reviews_enabled": false,
    "wikipedia_enabled": false
  },
  "series": [
    {
      "series": "郵便局",
      "color": "#dc2626",
      "borderColor": "#b91c1c",
      "size": 26,
      "label": "〒",
      "textColor": "#ffffff"
    }
  ]
}
```

- `key`/`label`は必須。`settings`は省略可(省略したキーは既定値のまま — 既定値は
  travel-log側の`lib/types.ts`の`SPOT_TYPE_SETTING_DEFAULTS`参照。現時点では
  `public_visible`が既定`false`、`reviews_enabled`/`wikipedia_enabled`が既定`true`、
  `region_scope`が既定`"jp"`、`wikipedia_lang`が既定`"ja"`)。
  種別追加時は基本的に非公開(`public_visible`既定false)で始める運用のため、
  `public_visible`は明示せず省略するのが基本(明示するのは既定と異なる値にしたい
  設定のみでよい。上の例も既定と同じ`public_visible`は書いていない)
- 日本以外を対象にした種別は`settings`に`region_scope`を指定する(`"jp"`=日本/
  ISO 3166-1 alpha-2の国コード小文字=その国/`"world"`=世界全体)。CSVの`region`列に
  入れる値がこれに連動する(日本=都道府県、国指定=州・県、世界=国名)ほか、
  地域タブの名称と並び順・地名検索の対象国・地図の初回表示も変わる。日本語以外の
  Wikipedia記事を引きたい場合は`wikipedia_lang`(例: `"en"`)も併せて指定する。
  詳細と海外データ作成時の注意はCLAUDE.mdの「region_scope(対象地域)と海外データ」節を参照
- `series`も省略可。このスポット種別で使えるシリーズの一覧と、それぞれの表示スタイル
  (バッジ・地図ピン共通)を配列で指定する。配列の順序がそのままシリーズの並び順
  (絞り込みチップ・一覧のソート順)になる
  - `series`: シリーズ値そのもの(`spots.series`に入る自由入力の文字列)
  - `color`: 背景色(`#rrggbb`)
  - `borderColor`: 縁取り線の色。非公開スポットはこの色のまま破線になる(それ以外は
    公開スポットと同じ実線)
  - `size`: 地図ピンの大きさ(px)。バッジの大小はこれと無関係(表示側の`size`propで別管理)。
    `series`が重要度・段階を表さない種別(放送回・企画名など)は全シリーズ26(観光地Aシリーズと同じ)に
    統一し、`series`が重要度・段階を表す種別(観光地のA〜Eなど)だけ上位ほど大きくする
  - `label`: バッジ・ピンに表示するラベル。文字列、または`{ "image": "data:image/png;base64,..." }`
    形式の画像(base64)のどちらか
  - `textColor`: 省略可。ラベルが文字列の場合の文字色。省略時は`color`の明度から
    自動で白/濃色を選ぶ(画像ラベルの場合は無視される)
  - `series`自体を省略した場合(または管理画面の手入力フォームで種別を追加した場合)は、
    観光地の現行A〜E配色がそのまま既定のシリーズ設定になる
    (travel-log側`lib/seriesStyle.ts`の`DEFAULT_SERIES_STYLES`参照)
- `categories`も省略可。このスポット種別で使うカテゴリの一覧(文字列配列。シリーズと違い
  見た目の指定は無い)。配列の順序がそのままカテゴリの並び順(地図・スポット一覧の
  カテゴリ絞り込みチップと、スポット追加・編集フォームのサジェストの並び)になる。
  CSVの`categories`列の値と一致させる(未定義の値でも動くが、並びは一覧の後ろになる)
  - `categories`自体を省略した場合(または手入力フォームで種別を追加した場合)は、
    観光地の現行カテゴリがそのまま既定になる
    (travel-log側`lib/categories.ts`の`DEFAULT_CATEGORIES`参照)。
    空配列`[]`を明示すると「定義済みカテゴリなし」(既存スポットの値だけが
    絞り込み・サジェストに出る)になる
- 取り込みはtravel-log側の管理画面(`/[type]/admin`の「別のスポット種別の管理」)から
  このJSONファイルを手動アップロードして行う。スポットデータ(CSV)とは別工程で、
  先にこのJSONで種別を作成してから、CSVをその種別のページでインポートする想定
- `public_visible`が`false`(既定)で作成された種別は、CSVインポート・内容確認が終わってから
  管理画面の「スポット種別の設定」で`true`に切り替えて一般公開する

## 各データの出典

- `tourist/spots.csv`(10,711件): Wikipedia(ja)の**「○○の観光地」47都道府県ページ**を唯一の
  スポット候補ソースとして機械生成したもの(travel-log本体には同梱せず、このリポジトリでのみ
  管理する。経緯は上記「データの出典・ライセンス表示」参照)。かつてOverpass API(OSM)から
  一括取得していた版は、観光地ページに載っていないスポットを大量に含む一方で重要スポットの
  取りこぼしもあったため、全件を作り直した。

  必訪シリーズ(A〜E)は主観ではなく、**Wikipedia(ja)の月次ページビュー数**(bot除外)を知名度の
  客観指標としてパーセンタイル区分している(生成当時の観光地11,628件中の相対順位)。最上位をSにすると
  運用上何かと面倒なため、A〜Eの5段階にしている。

  | シリーズ | 相対順位 | 目安 |
  |---|---|---|
  | A | 上位5% | 全国的に絶対外せない |
  | B | 次15% | 全国区で有名 |
  | C | 次30% | 地方の定番 |
  | D | 次30% | 地元で知られている |
  | E | 残り20% | 穴場・マニアック向け |

  生成手順(スクリプトはリポジトリに含めていない一時作業):

  1. 「○○の観光地」47ページのwikitextから記事内リンク20,347件を抽出(テンプレート由来の
     ナビゲーションリンクを拾わないよう、レンダリング後のリンク一覧ではなくwikitextを使う)
  2. リダイレクトを解決して同一記事を1件に統合し、赤リンク・曖昧さ回避・一覧ページ・
     祭事/イベント・作品(ドラマ等)・市区町村そのものの記事・都道府県そのものの記事、
     および**代表点1つでは表現できない広域地物**の記事を除外(次項「収録する場所の単位」)
  3. 座標はWikipedia記事の位置情報→Wikidata `P625`の順に採用し、どちらにも無いものは除外。
     当初はさらにOSM(osm_japan、同名地物の完全一致のみ)で座標を補完していたが、touristから
     ODbL(OpenStreetMap)の表示義務を外すため、2026-07-23に全件をMediaWiki Action API・
     Wikidata APIと突き合わせ直し、座標がWikipedia記事座標・Wikidata `P625`のいずれとも
     一致しない行(OSM由来の補完分など)を除外した。現在の座標の内訳は**Wikipedia記事座標
     9,282件・Wikidata `P625` 1,501件(計10,783件)**で、全件がWikipedia記事座標または
     Wikidata `P625`(±0.001度以内)と一致する(この2026-07-23再生成時点の件数。以降、
     令制国6件・路線そのもの2件(東名高速道路・箱根ターンパイク)を2026-07-26に除外、さらに
     同一座標で重なるスポット38件(横浜みなと博物館ほか。妙高戸隠連山国立公園は分離元の上信越
     高原国立公園へ)を同一地点の主要スポットへ統合、線状の別子ライン1件と、出典の座標自体が
     誤っていたアトア1件を除外し10,736件。一部の同一座標(横浜開港資料館と神奈川県立歴史
     博物館など)は座標を精緻化して分離した。さらに2026-07-26のシリーズ点検の際に、既存基準の
     除外漏れだった全国チェーン企業・訪問先の無い企業そのもの・閉業済み施設の12件
     (ビックカメラ・味の素・三井アウトレットパーク 大阪鶴見ほか)を除外し10,724件、
     同日の再点検で閉業・解体済みで現存しない施設12件(ノースサファリサッポロ・
     広島市民球場(初代)ほか)と観光道路の路線そのもの1件(磐梯吾妻道路)をさらに除外し
     現在10,711件)
  4. `description`はWikipedia記事の冒頭1文、`name_kana`は冒頭文の読み仮名から機械抽出。
     さらに`description`の末尾に、その場所の**肩書き(国の指定・登録・選定)**を
     `世界遺産・国宝・特別史跡・日本100名城・日本さくら名所100選。`のように追記している
     (3,180件=全体の29%に付与。次項「肩書きの付け方」参照)
  5. `region`は冒頭文中の都道府県名(無ければ掲載元ページの都道府県)。座標が
     その都道府県の範囲から外れるものは、県内の同名OSM地物で座標を取り直すか、
     座標側に合わせて`region`を付け替える
  6. `series`は月次ページビューの全国パーセンタイル、`categories`は記事名・冒頭文の
     キーワードによる機械分類のため、いずれも精度にはばらつきがある

  収録する場所の単位(「1つの座標で指せる訪問先か」を基準にした線引き。手動でスポットを
  追加するときも同じ基準で判断する):

  | 種類 | 扱い |
  |---|---|
  | 市区町村・郡・都道府県そのもの | **除外**(訪問先ではなく地域区分) |
  | 河川本流・運河・用水路 | **除外**(線状地物。渓谷・峡・河川敷公園など地点性のあるものは収録) |
  | 湾 | **原則除外**。ただし湾そのものが名勝・遊覧・アクティビティの目的地になっているものは例外として収録(川平湾・英虞湾・伊根湾・須佐湾・慶佐次湾・久美浜湾の6件) |
  | 半島・諸島・列島・群島 | **除外**(広域区分。半島内・島ごとの個別スポットは収録) |
  | 島 | **収録**。ただし定期船・遊覧船・上陸ツアーのいずれも無い無人島(小笠原の兄島・嫁島・南硫黄島など)は除外 |
  | 現代の幹線道路・有料道路(甲州街道・青梅街道・東京湾アクアライン・○○ラインなど) | **除外**(移動のための道路。全長が長く、どこに座標を打っても意味を持たない) |
  | 古道・参詣道・遊歩道・観光鉄道(熊野古道・大峯奥駈道・哲学の道・大井川鐵道など) | **収録**(歩く・乗ること自体が目的の観光資源。線状でも上記の道路とは区別する) |
  | 峠・宿場・通り・並木(美幌峠・妻籠宿・竹下通り・日光杉並木) | **収録**(峠は展望地点、宿場・通りは一地区として1つの座標で指せる) |
  | 再開発地区・複合商業施設(六本木ヒルズ・東京ミッドタウン・みなとみらい21・神戸ハーバーランドなど) | **収録**(商業・飲食・展望・イベント・パブリックアートなど一般来訪者向けの機能を持つ訪問先。街区型でも宿場・通りと同様に一地区として1つの座標で指せる。「観光地らしさ」の度合いは収録可否には使わず、知名度の軸(シリーズA〜E)に任せる — 2026-07-26に再開発由来の収録スポットを全件点検し、訪問先性の無いものは無かったため全件残した) |
  | 作品・イベント・行事(ドラマ・コンテスト・民俗芸能・年中行事等) | **除外**(場所ではない) |
  | 人物・事件・企業そのもの | **除外**(場所ではない。ただし記事の主語が運営会社でも、その座標に一般公開の施設(酒蔵・牧場・工場見学・劇場・観光鉄道)があるものは収録) |
  | 全国チェーンの企業・ブランド(コストコ・パルコ等) | **除外**(1つの座標で指せない) |
  | 閉業・解体済みで現存しない施設(記事の冒頭文が過去形のもの) | **除外**(現存しない訪問先)。ただし跡地・遺構が史跡・保存建築として公開されているもの(増毛駅・石巻市立大川小学校・美濃駅・箱根関・北沢浮遊選鉱場など)は収録 |

  初回生成では市区町村・祭事の除外に取りこぼしがあり(神奈川区・新宿区・唐津くんちなど)、
  広域地物にはそもそも基準が無かったため、後から上表の基準で538件を除外した
  (11,628→11,090件)。**大学・ゴルフ場は「訪問先か」の判断がスポットごとに割れるため、
  今回は一律に残してある**(北海道大学のように観光地として定着している例があるため)。
  CSVから外したスポットの`key`は`tourist/excluded_candidates/exclude.txt`に
  追記してあり、travel-log側の`/[type]/admin`「キー一覧を指定して削除」に貼り付けると
  DBからも消せる(CSVの再取り込みは差分更新のため、行を消しただけではDBには反映されない)。
  除外した候補とその理由は`tourist/excluded_candidates/`に都道府県別に残してある
  (取りこぼしを後から検証・救済できるようにするため)。世界遺産・国宝等の指定があるが
  ページビューが伸びにくい場所は、目視で格上げする例外を許容するハイブリッド方式
  (完全自動ではない)。逆に、**受験・就活・企業情報・日常の買い物など、ページビューの
  由来が観光でないスポット(大学・企業・商業施設)は目視で格下げする**(2026-07-26に
  A・Bの該当133件を全件点検し64件を格下げした。個々の変更と理由・据え置きの一覧は
  `tourist/excluded_candidates/series_demotions.md`参照)。travel-log側で
  手動でスポットを追加する場合も、この基準を参考にシリーズを付けるとブレにくい。

  肩書きの付け方(`description`末尾の「なぜ観光地なのか」):

  - 出典はWikipedia記事の**カテゴリと冒頭文**で、LLMによる生成文は使っていない
    (検証できない文をデータに混ぜないため)。強い順に最大5個まで`・`で連結する
  - 対象は制度に基づく指定・登録・選定のみ: 世界遺産/国宝/特別史跡・特別名勝・特別天然記念物/
    重要文化財/重要伝統的建造物群保存地区/重要文化的景観/国の史跡・名勝・天然記念物/
    登録有形文化財/日本遺産/近代化産業遺産/ラムサール条約登録湿地/ジオパーク/国立・国定公園/
    日本100名城/日本百名山・日本百景・○○100選(百選)の各種選定
  - 上位互換がある場合は下位を落とす(特別史跡があれば国の史跡は書かない、国宝があれば
    重要文化財・登録有形文化財は書かない)
  - **博物館・美術館・資料館・記念館は、カテゴリだけを根拠に国宝・重要文化財を付けない**
    (収蔵品の指定を自館の肩書きとして拾ってしまうため。冒頭文に建物自体の指定が
    書かれている場合のみ付ける)
  - 付与率は29%。動物園・水族館・展望台・スキー場・テーマパーク・道の駅・繁華街は
    そもそも指定制度の対象外なので肩書きが付かない。**肩書きの有無は観光地としての
    価値とは別の軸なので、収録可否の基準には使わない**(知名度の軸はページビューの
    A〜Eが担っている)
- `post_office/spots.csv`: 国土交通省 国土数値情報(郵便局データ P30、平成25年度版)。
  行政区域コードを都道府県名・市区町村名に変換、郵便局分類コードを`categories`列に変換した加工版。
  非商用利用限定のデータセットである点に注意(travel-log側のCLAUDE.md「外部データソースを
  扱う際の注意」も参照)。
- `goshuin/spots.csv`(51,950件): OpenStreetMap(Overpass API)の寺社仏閣
  (`amenity=place_of_worship`かつ`religion=shinto`/`buddhist`)を唯一のスポット候補ソースとして
  機械生成したもの。かつてランク付き(`goshuin_ranked.csv`)と未整理(`goshuin_unranked_z.csv`)の
  2ファイルに分かれていたものを、1ファイルに統合して全件を作り直した(Wikipedia記事との
  突き合わせが甘く、人物記事や宗教団体の記事にぶら下がった誤ランクが混ざっていたため)。

  シリーズ(A〜E)はtouristと同じく**Wikipedia(ja)の月次ページビュー数**(bot除外)の
  パーセンタイル区分で、Wikipedia記事に紐付かない寺社は`Z`(未整理)。御朱印は無名の寺社でも
  受けられるため、`Z`も落とさず同じファイルに収録している。

  | シリーズ | 件数 | 内容 |
  |---|---|---|
  | A〜E | 6,995件 | Wikipedia記事と紐付き、ページビューでランク付けできたもの |
  | Z | 44,955件 | Wikipedia記事が無く、OSMに名前付きで採録されているだけのもの |

  生成手順(スクリプトはリポジトリに含めていない一時作業):

  1. Overpassで47都道府県の`admin_level=4`エリアごとにnode/way/relationを取得(境内が面で
     描かれている寺社を取りこぼさないため。way/relationは重心座標を採用)。`region`列は
     この絞り込みで確定するので、座標からの空間検索による近似判定は不要になった
  2. 同名かつ200m以内の要素(建物way+POI nodeなど)を1件に統合
  3. OSMの`wikipedia`タグ、無ければ`wikidata`タグ経由でja記事名を解決
  4. その記事が**本当にその寺社の記事か**をchiezo(jawikiのローカルミラー)で検証し、
     外れたものはランク付けせず`Z`に落とす:
     - 記事が実在する(リダイレクトは解決する)
     - 冒頭文が寺社の記事である(人物・宗教団体・自治体・駅などを除外。例:
       「崇徳天皇御廟」→崇徳天皇本人の記事、「立正佼成会」→宗教団体の記事)
     - 冒頭文に都道府県名が出る場合、OSM上の所在県と一致する(分社が総本社の記事を
       指しているケースを除外)
     - 「稲荷神社」のような社名全体を説明する総称記事にぶら下がっていない
     - 同じ記事を複数のスポットが指す場合、記事名と一致する1件だけを採用(曖昧なら全て`Z`)
  5. ページビューはchiezoがWikimediaの月次ダンプ(`pageview_complete`、bot除外)から
     取り込んだ値を使い、A=上位5%/B=次15%/C=次30%/D=次30%/E=残り20%で区分。
     世界遺産の構成資産などページビューが伸びにくい寺社は目視で格上げする例外を許容する
     (touristと同じハイブリッド方式)
  6. `description`はWikipedia記事の冒頭文(200字を目安に文単位で切り出し)、`name_kana`は
     冒頭文の読み仮名から機械抽出(記事名と名称が一致する場合のみ)。名称のうち英語併記
     (`猿田彦神社 (Sarutahiko Shrine)`など)は日本語表記だけに整形
  7. ランク付きスポットと100m以内で近接する`Z`(同一寺社の別名タグ等、2,520件)は除外
- `buratamori/spots.csv`: NHK「ブラタモリ」レギュラー版全5シリーズ(第1シリーズ2009年〜
  第5シリーズ放送中)について、放送回ごとの訪問地(都道府県・市区町村)はjawikiの番組公式記事
  「放送日程」節を参照した上で、具体的な立ち寄りスポット名・そのスポットの回内での役割の
  説明文は一般知識から作成した試験データ。座標のみosm_japanで裏取り済み。アンコール放送・
  完全版・過去回の再構成スペシャルなど新しいロケ地情報が無い回は対象外。`series`列は
  `description`冒頭と同じ`【#9 品川】`形式(放送回番号+タイトル。
  スペシャルは`【#SP1 ...】`のような表記)。放送回番号(`9`のような数字)だけだとシリーズが
  変わると1話から数え直すため種別全体では一意にならず、番号だけをseriesにすると別シリーズの
  同じ番号の回が同じ見た目・同じ絞り込み対象になってしまう。そのためseriesはタイトルまで
  含めた文字列にして種別全体で一意にし、ピンに表示する短い番号は`settings.json`の`series`側の
  `label`に分離してある。前後編にまたがっていて前編・後編のどちらで登場したか分からない
  スポットは、`【#17-18 ...】`のような合体表記ではなく**前編側の1回(`【#17 ...】`)に寄せる**
  (かつて合体表記と単独表記が混在していたため統一した。#17-18・#21-22・#32-33・#38-39の4件が
  該当)。`categories`列は
  `第<N>シリーズ`(1〜5)でシリーズを表す。`settings.json`の`series`は全340回(5シリーズ合計)を
  放送順に列挙した機械生成(見た目は全話共通、ピンには`label`の番号のみ表示)。
  この340回にはスポット行を持たない回も6回含まれる(他の回のスポットから
  「【#…】でも訪れている」と参照されるだけの回。第3シリーズ【#16 新宿誕生編(前編)】、
  第5シリーズ【#10 東京・青山通り…】・【#28 初詣スポット 川崎大師…】と、重複統合で
  スポット行が他の回に寄せられた3回)。
  **具体的なスポット名・説明文・各回との対応関係は未検証(要確認)。** シリーズごとに別々の
  調査(別セッション)で作成したものを1ファイルに統合したため、同じ場所を複数シリーズ・
  複数放送回にわたって再訪しているケースがある(例: 東京大学赤門は第1・第2・第5シリーズで
  計3回登場)。travel-log側の取り込みが`name`+`lat`+`lng`の完全一致で重複除外する
  ため、CSV側でも先に訪れた回の行だけを残し、あとの回に訪れたことは残した行の
  `description`末尾に追記する形で統合済み(重複行として残してはいない)。`key`列は
  スポット名(名前重複時のみ`名前_2`の連番サフィックス)で全406件に付与
- `buratamori/routes.csv`(3ルート): **巡回順が公表情報から裏取りできる回だけ**に絞ってある。
  かつては全56ルートを「spots.csvの回ごとの行順をそのまま`seq`にする」機械生成で持っていたが、
  行順は巡回順の根拠になっておらず(同一敷地内を結ぶ矢印すら含んでいた)、
  「巡った順」を騙る誤情報になるため廃止した。残した3ルートと、その順序の根拠:
  - `伊勢神宮への旅(第5シリーズ#1〜#5)`: 5夜連続で桑名→四日市→鈴鹿→津→松阪→明和→伊勢と
    伊勢街道を南下する旅企画。夜ごとの訪問地(市町村)の並びがjawikiの放送日程節に載っており、
    回をまたいだ進行方向が確定する。回内の並びも街道の南下順に合わせてある
    (松阪市内の2件だけは前後関係が不明のため、街道の進行方向に沿う順に置いただけ)
  - `富士山登山(第4シリーズ#19〜#21)`: jawikiの訪問地欄が
    「富士宮口五合目〜六合目」→「六合目〜宝永火口」→「九合五勺〜山頂〜剣ヶ峰」と
    登山の進行そのものを記載しており、回をまたいだ順序が確定する
  - `大山詣り(第5シリーズ#12〜#13)`: 前編が中腹の下社、後編が「山頂へ」で本社。登拝順が確定する
  上記以外の回は、立ち寄りスポットが1件しか採録されていないか、回内の順序を裏取りできないため
  ルートを持たない(`spot_key`から参照されないスポットが大半になるが、`key`列自体は
  再アップロード時の同一判定に使うため全件に付けたままにしている)
- `suiyou_dodesho_domestic/spots.csv`: HTB(北海道テレビ)「水曜どうでしょう」のレギュラー放送時
  (1996年〜2002年)の**国内企画のみ**を対象にした試験データ(海外企画は対象外。別キーで海外編を
  追加する想定のため、意図的に`suiyou_dodesho_domestic`という国内限定であることが分かる
  キー名にしている。travel-log側の海外対応は実装済みなので、海外編を追加する場合は
  `settings.json`の`region_scope`を`"world"`にした別フォルダを作り、`region`列に国名を入れる)。
  jawiki記事「水曜どうでしょうの企画 (日本国内)」の
  本文から、企画中に立ち寄った・通過した地点を一般知識で抽出した試験データ。観光名所に
  限らず、サイコロの目で決まっただけの経由地・宿泊地・道の駅・サービスエリア・フェリー
  乗り場なども対象に含む(地名が特定できない曖昧な記述のみ対象外)。座標はosm_japanで
  裏取りするが、**osm_japan(chiezo)には鉄道駅・空港・港・IC/SAが入っていない**ため、
  それらはNominatimで取得している。市町村・字・廃業済みの施設など点で特定できないものは
  自治体や近隣地物の代表点で代用し、その旨を`description`末尾に書いてある
  (例: 上発知のしだれ桜=沼田市上発知町の代表点、HTB=企画当時の南平岸旧社屋の代表点)。
  `series`列には企画名
  (例: `サイコロ4〜日本列島完全制覇〜`)が入る。放送回数の情報が取れないため、
  シリーズ=放送回の代わりにシリーズ=企画としており、`settings.json`の`series`が企画ごとに
  2文字以内の略称ラベル(例: `サ4`。地図ピン・バッジに表示される)と系列ごとの色
  (サイコロ・釣りバカ・試験に出る等は同色)を定義している。並び順はjawiki記事の
  企画の掲載順(=CSVの出現順)。
  `categories`列は当初`series`列と同じ企画名だったが、企画数(37)がそのままカテゴリ数に
  なって絞り込みチップが見づらかったため、**`サイコロ`・`北海道企画`・`その他`の3つに
  整理し直した**。`サイコロ`=サイコロ1〜6、`北海道企画`=道内で完結する企画
  (カントリーサインの旅I・II、十勝二十番勝負、釣りバカ対決各回、粗大ゴミで家を作ろう、
  30時間テレビ、カルトクイズ世界大会、シェフ大泉各回、onちゃんカレンダー)、残りが`その他`。
  道外へ出る企画(闘痔の旅=北海道→青森 など)は北海道発でも`その他`に入れている。
  企画そのものは`series`(=ピンの色・ラベル)で引き続き区別できる。
  **具体的なスポットの採否・説明文・各企画との対応関係は未検証(要確認)。** 2つのパートに
  分けた別々の調査を1ファイルに統合したため、同一スポットが複数企画に登場するケースが
  稀にある(例: 厳美渓は「桜前線捕獲大作戦」「東北2泊3日生き地獄ツアー」の両方に登場)。
  buratamoriと同様、先に登場した企画の行だけを残し、あとの企画に登場したことは
  `description`末尾に追記して統合済み。`key`列はスポット名(名前重複時のみ`名前_2`の
  連番サフィックス)で全269件に付与。内訳の経緯:

  - 37件は後から追加したもの。うち22件はルート作り直し(後述)の際に、jawikiの行程節に
    地名があるのに未収録だったスポット(サイコロ1・2の出発地の六本木プリンスホテル、
    サイコロ3後編の津名港・神戸ヘリポート・但馬空港、原付東日本のカブ購入店・竹田輪業ほか)。
    残り15件は、**企画そのものが
    まるごと未収録だった6本**を追加したもの:「日本全国絵ハガキの旅」「日本全国絵ハガキの旅2」
    「第1回水曜どうでしょうカルトクイズ世界大会」「シェフ大泉 車内でクリスマス・パーティー」
    「釣りバカ対決!わかさぎ釣り2」「onちゃんカレンダー」。この6本の追加に伴い
    `settings.json`の`series`/`categories`も31→37企画に増え、釣りバカシリーズのラベルは
    放送順(門別=釣1/氷上わかさぎ=釣2/わかさぎ2=釣3/屋久島=釣4)に振り直した
  - スポット行を持たない企画が3本ある(シェフ大泉 車内でクリスマス・パーティー=HTB駐車場、
    onちゃんカレンダー=HTB第1スタジオ+ハワイ・ラスベガス、釣りバカ対決!わかさぎ釣り2=新篠津村。
    いずれも会場が他企画のスポットと同じ地点のため、そちらの`description`末尾に追記する形にした)
  - 削除・付け替えたもの(いずれも旧keyは
    `excluded_candidates/exclude.txt`に理由付きで記載):
    `利島`は「ゴールデンスペシャル サイコロ6」で**サイコロの選択肢に挙がっただけで実際には
    訪れていない**ため削除、`バスタ新宿`は企画当時(1997年)に存在しない施設名だったため当時の
    `新宿高速バスターミナル`として付け直し、`苫小牧港_2`は`苫小牧港`と同じ地点のため統合
  - `新千歳空港`は「サイコロ3」後編で先に登場しているのに`series`が「サイコロ4」のままに
    なっていたため、「スポットを追加するとき」のルール(先に登場した企画に属させる)どおり
    `サイコロ3〜自律神経完全破壊〜`へ付け替え、`description`もサイコロ3を主語に書き直した
    (サイコロ4・5のことは末尾への追記に回した)
  - `大町温泉郷`(長野県大町市)は、サイコロ4で信濃大町駅からレンタカーで伊勢神宮へ向かう
    途中の立ち寄り先。jawikiの行程節には出てこないため未収録だったものを追加した
    (座標は大町温泉郷バス停=Nominatimで代用)
  - `東京2泊3日70km`のラベルは、数字だけで何の企画か分からない`70`から`東京`に変更した
- `suiyou_dodesho_domestic/routes.csv`(30ルート): jawiki記事の企画ごとの
  「行程」節・「旅程」節・日別の見出し(原付シリーズ・対決列島など)に**書かれている順番**を
  そのまま`seq`にしたもの。かつてはspots.csvの行順を機械的に`seq`にしていたが、
  行順が移動順になっていない企画が多く、**旅の出発地(東京)がまるごと抜けている**という
  問題があったため、記事の行程に沿って全ルートを作り直した。作り直しで直った主なもの:
  - サイコロ1・2の出発地(六本木プリンスホテル→新宿高速バスターミナル/東京駅)、
    サイコロ5・6の羽田空港以降の深夜バス区間、東京2泊3日70kmの出発地(羽田空港)、
    原付東日本の出発地(銀座)、桜前線捕獲大作戦の出発地(仙台・西公園)を追加
  - 移動の途中にある地点が末尾に置かれていたものを本来の位置へ移動
    (サイコロ2の壇之浦PA=はりまや号の休憩地、サイコロ4の千歳IC・新千歳空港=冒頭のドッキリ、
    闘痔の旅の函館港・青森港、十勝二十番勝負の日勝峠、カントリーサインの旅の寄り道4地点、
    原付東日本のだるま屋ウィリー事件現場、対決列島の天文館むじゃき など)
  - 四国八十八ヵ所は札所番号順(I・IIは順打ち、IIIは逆打ち)に整列
  - 1回のロケで同じ地点を2度通る企画も、**1ルート内で同じ`spot_key`を何度でも使える**ため
    分割しない(サイコロ6は`第1夜`/`第2夜`の2ルートに分けていたが、その分け方だと
    松山空港→羽田空港の移動が抜けるため、羽田空港・新大阪駅・高知空港をそれぞれ2回経由する
    1本の`ゴールデンスペシャルサイコロ6`に統合した)。サイコロ3を`前編`/`後編`のまま
    残しているのは、放送自体が前後編に分かれ、後編が別ロケ(札幌から再出発)として
    始まるため
  - 各ルートの`leg_description`列に移動手段(深夜バス・特急・フェリー・原付カブ・車・徒歩など)を
    記入した。またクイズ!試験に出るどうでしょうは企画がHTBスタジオ発のため、先頭に既存スポット
    `北海道テレビ放送`(南平岸旧社屋)を経由地として追加した(ルート数は31本のまま)。原付東日本の
    カブ購入店は`銀座`から実際の店名`竹田輪業`(中央区)へ、湯沢温泉は関川村付近から`越後湯沢温泉`の
    座標へ修正した。原付東日本は出発地`銀座`を銀座四丁目交差点に置き直し、カブを購入した築地の
    バイク店`竹田輪業`を2番目の経由地として新規追加した(湯沢温泉→越後湯沢の`key`は`湯沢温泉`のまま
    でルート参照を維持)
  **行程節に地名が書かれていない立ち寄り先(宿・対決場所など)までは網羅していない。**
  未収録と分かっているもの: 闘痔の旅の1軒目「薬師温泉(ニセコ町)」、東京2泊3日70kmの
  多摩川緑地、5周年記念…の出発地「大通バスセンター」。また企画そのものが未収録のものとして
  なお経由地が1件以下の企画(門別沖釣りバカ対決・氷上わかさぎ釣り対決・わかさぎ釣り2・
  シェフ大泉 夏野菜スペシャル・シェフ大泉 車内でクリスマス・パーティー・
  第1回水曜どうでしょうカルトクイズ世界大会・onちゃんカレンダー・
  30時間テレビの裏側全部見せます!)は矢印が引けないためルートを持たない
- `suiyou_dodesho_overseas/spots.csv`: 上記`suiyou_dodesho_domestic`の海外編。jawiki記事
  「水曜どうでしょうの企画 (海外)」に載っているレギュラー放送時(1996年〜2002年)の海外企画
  全11本(オーストラリア大陸縦断3,700キロ・韓国食い道楽サイコロの旅・ヨーロッパ21ヵ国完全制覇・
  マレーシアジャングル探検・香港大観光旅行・北極圏突入〜アラスカ半島620マイル〜・
  アメリカ合衆国横断・ヨーロッパ・リベンジ・中米コスタリカ・ユーコン川160キロ・
  原付ベトナム縦断1800キロ)を収録、全111件、対象国21ヵ国。各企画の「旅程」節に載っている
  地名から、明確に特定できる主要な立ち寄り地のみを抜粋した試験データ(通過しただけの曖昧な
  地点・詳細不明な地点は対象外)。`region_scope`は`"world"`で、`region`列には国名(日本語)を
  入れている。座標はchiezo(日本限定)ではカバーできないため、外部のNominatim(OpenStreetMap
  公式の地名検索API)を識別可能なUser-Agent・数秒間隔のレート制限付きで直接叩いて取得した
  (「外部データソースを扱う際の注意」参照)。`series`列には企画名が入り、
  `settings.json`の`series`に企画ごとの色・ラベル(2文字以内の略称、例: `欧1`)を定義する
  (suiyou_dodesho_domesticと同じ設計)。`categories`列は国内編と同じ理由(企画数がそのまま
  カテゴリ数になって絞り込みが見づらい)で、**全件`海外企画`の1カテゴリに統一している**
  (海外編は行き先がとりとめなく、国内編の`北海道企画`のようなまとまりを作れないため)。buratamori・suiyou_dodesho_domesticと同様、
  複数企画で同じ場所を再訪しているケース(「ヨーロッパ21ヵ国完全制覇」と「ヨーロッパ・
  リベンジ」で共に凱旋門がスタート地点)は先に登場した企画の行だけを残し統合済み。
  **具体的な立ち寄り順・説明文は一般知識による推定を含み未検証(要確認)。** レギュラー放送
  終了後の不定期企画(「ヨーロッパ20ヵ国完全制覇〜完結編〜」「21年目のヨーロッパ21ヵ国
  完全制覇」等)は上記jawiki記事の対象範囲外のため未収録。`key`列はスポット名
  (全111件で重複なし)で付与。うち32件は後述のルート作り直しの際に、旅程節に地名があるのに
  未収録だったスポット(ヨーロッパ21ヵ国完全制覇のチロル=オーストリア、ヨーロッパ・リベンジの
  ストラスブール以降のドイツ区間、アメリカ合衆国横断の各州の宿泊地ほか)として追加したもの
- `suiyou_dodesho_overseas/routes.csv`(11ルート): suiyou_dodesho_domestic/routes.csvと同様、
  jawiki記事の企画ごとの「旅程」節に**書かれている日付順・地名順**をそのまま`seq`にしたもの。
  かつてはspots.csvの行順を機械的に`seq`にしていたため、国内編と同じく出発地や経由地が
  抜けていた。作り直しで直った主なもの:
  - 出発地の欠落: ヨーロッパ・リベンジ(パリ凱旋門から始まるのにザバブルク城が起点になっていた)、
    香港大観光旅行(啓徳空港・中環が無くヴィクトリア・ピークが起点だった)、
    北極圏突入(乗り継ぎ地シアトルが無かった)
  - **訪問国そのものの欠落**: ヨーロッパ21ヵ国完全制覇でオーストリア(チロル)を通過している
    のにルートにも`region`にも現れていなかった(これで対象国が20→21ヵ国になった)
  - 経由地の欠落: アメリカ合衆国横断の宿泊地(クリントン・バッファロー・マリオン)と
    通過地ナッシュビル、オーストラリア縦断の初日2地点、マレーシアのクアラ・テンベリン、
    ユーコン川のビッグ・サーモン(110km地点)、ベトナム縦断のタインホア・ドンホイ・ダナン・
    クイニョン・ファンラン、ヨーロッパ21ヵ国のフォークストン・ブローニュ・ペルピニャン・
    ブール=マダム
  全11企画とも経由地2件以上のため全企画を収録。**旅程節に地名が無い立ち寄り先までは
  網羅していない**(マレーシアの動物観察小屋ブンブン・ブラウ、コスタリカの宿など)。また
  「ヨーロッパ21ヵ国完全制覇」でモナコ・アンドラに入国したことが本文に書かれているが、
  旅程節では経路が特定できないため未収録(要確認)
- `restaurant/spots.csv`(171件): 有名飲食店のデータ。**候補ソースは2系統**あり、
  jawiki(ja Wikipedia)の**飲食店系のタグ(カテゴリ)**に属する記事(113件)と、
  osm_japan(OSM)側の**`wikipedia`/`wikidata`タグが指すjawiki記事**(58件)からなる。
  ジャンル別の内訳はラーメン48、日本料理・料亭30、喫茶・カフェ25、洋食16、そば・うどん14、
  その他13、菓子・パン12、丼もの・定食5、寿司3、中華3、カレー1、焼肉1。

  タグ側の候補ソースは、ラーメンが`ラーメン店`(131件)、それ以外が**都道府県別の
  `○○の飲食店`39タグ(373件)**と**ジャンル別タグ**(`うどん・蕎麦屋`77・`寿司屋`63・`料亭`49・
  `ハンバーガー店`49・`カレー店`26・`焼肉店`23・`ステーキハウス`18・`天ぷら店`11・`とんかつ店`10・
  `喫茶店`10・`菓子店`5ほか)。重複を除いて735件を調べた。**「寿司店」「そば店」「中華料理店」
  「居酒屋」という名前のタグは存在しない**(寿司は`寿司屋`、そば・うどんは`うどん・蕎麦屋`に
  まとまっている)ので、`tags?prefix=`で実在するタグ名を確かめてから列挙すること。

  生成手順(スクリプトはリポジトリに含めていない一時作業。すべてchiezoへの問い合わせで完結):

  1. **jawikiに記事があること自体を「有名店」のフィルタに使う**(チェーンの一般店舗には記事が無い)。
     列挙は`filter?tag=<タグ名>`。`total`と`offset`でページングする。
     **本文の全文検索(`search?q=Category:ラーメン店`)で列挙してはいけない** — カテゴリに
     ソートキーが付いた記事(wikitextで`[[Category:ラーメン店|らあめんしろう]]`と書かれたもの)は
     本文側からカテゴリ名が落ちるため取りこぼす。初回生成ではこの方法で115件しか拾えておらず、
     **ラーメン二郎**(月17,677PV、Aシリーズ相当)を含む17件が漏れていた
     (2026-07-27にchiezo側の`filter?tag=`対応を受けて列挙し直し済み)
  2. **逆方向にOSM側からも引く**(2026-07-30に追加)。`filter?feature=amenity=restaurant`などで
     飲食系のPOIを全件ページングし、`wikipedia`/`wikidata`タグを持つ613件を抜き出して
     jawikiの記事210件に解決した。**タグ列挙だけでは飲食店を網羅できない** —
     名曲喫茶らんぶる・ジャズ喫茶ちぐさのように飲食店系タグに属していない記事や、
     OSMの店名が記事名と違う店(中清→`天麩羅中清`、上野精養軒→`精養軒`、
     並木藪蕎麦→`並木數喬麥`)は1系統では拾えない。詳細と除外理由は
     `restaurant/excluded_candidates/osm_wikipedia_sweep.md`
  3. `description`は記事冒頭の要約(1〜2文)。`name_kana`は冒頭文の読み仮名
  4. `series`(A〜E)はtourist・goshuinと同じく**月次ページビュー数**(bot除外、2026-06)だが、
     母集団が小さくパーセンタイルが安定しないため、下表の**絶対値のしきい値**で区切っている
  5. 座標はosm_japan(OSM)の店舗POI。jawikiの飲食店記事は座標を持たないことがほとんどで、
     Wikipedia/Wikidata由来の座標ではほぼ収録できないため(ライセンス上の帰結は
     「データの出典・ライセンス表示」参照)。突き合わせは店名+所在地(`area`)での検索と、
     OSM側の`wikipedia`タグの2通りで、**後者は「どのPOIがその記事の店か」をOSMが
     自己申告している**ぶん確実。本店の判定は`branch`タグ(`branch=本店`・`branch=道頓堀本店`)が
     決め手で、`branch=◯◯店`は支店なので弾く
  6. 座標の点検として、jawiki記事が座標を持つ55件と突き合わせた結果**53件が200m以内で一致**。
     不一致の2件(茶房長谷路・味仙 今池本店)はいずれもWikipedia記事側の座標が誤っており、
     CSV(OSM由来)が正しい

  | シリーズ | 月次ページビュー | 件数 | 目安 |
  |---|---|---|---|
  | A | 10,000以上 | 3 | 全国区で名前が通っている |
  | B | 3,000〜9,999 | 9 | 全国区のチェーン・名店 |
  | C | 1,000〜2,999 | 16 | 広域で知られている |
  | D | 300〜999 | 46 | ご当地の有名店 |
  | E | 300未満 | 97 | 地元・マニア向け |

  収録の単位(「1つの座標で指せる訪問先か」というtouristと同じ基準):

  | 種類 | 扱い |
  |---|---|
  | 単独店(記事の主語が1店舗) | **収録**。座標はその店 |
  | チェーンだが本店・創業店を記事から特定できる | **収録**。本店・1号店を代表点にし、`name`を`天下一品 総本店`のように店舗名まで含めて、`description`末尾にどの店の座標かを書く |
  | チェーン企業・ブランドそのもの(本店を特定できない) | **除外**(1つの座標で指せない。touristの全国チェーン除外と同じ) |
  | 料理・商品・運営会社そのものの記事 | **除外**(場所ではない)。ただし記事の主語が店(「◯◯は…にある和菓子店」)なら製菓・製パンの店も収録する。「◯◯株式会社は…メーカー」と企業を主語にしているものは除外 |
  | 飲食店が入っている施設・建築物の記事(道の駅・百貨店・登録有形文化財の邸宅など) | **除外**(記事が飲食店の記事ではない。大半はtourist側の対象) |
  | 閉店した店 | **収録可**(跡地・代表点が特定できる場合のみ。今回は該当4件とも座標を決められず未収録) |

  本店の特定は、OSMの`branch`タグ(`branch=本店`・`branch=秋葉原本店`など)と店名の`本店`表記を
  優先し、記事本文に書かれた本店の所在地(市区町村)と突き合わせて確定した。支店しか見つからない
  店は収録していない。収録しなかった候補と理由は
  `restaurant/excluded_candidates/`に残してある(`ramen.md`=ラーメン83件、
  `other_genres.md`=それ以外637件、`osm_wikipedia_sweep.md`=OSM起点で見つけた分の116件)。

  **ジャンルによって当たり方がかなり違う**点に注意。ラーメン店タグは記事の54%が
  チェーン企業の記事だったのに対し、都道府県別の`○○の飲食店`タグは12%しかなく、
  すきやばし次郎・煉瓦亭・神谷バー・ヨーロッパ軒のような単独の老舗・名店が中心になる。
  逆に`ハンバーガー店`・`寿司屋`・`ステーキハウス`はほぼ全国チェーンの企業記事で、
  `洋食`タグに至っては大半が料理そのものの記事(ナポリタン・オムライスなど)。
  ラーメン以外702件のうち座標を確定できたのは98件(+誤マッチを引き直した3件)、収録は65件で、
  **残り557件はOSMに該当POIが無い**(全国チェーンの企業記事と、閉店・POI未登録の店)。

  OSM側から引く経路では、飲食系POI 205,685件のうち`wikipedia`/`wikidata`タグを持つのは
  613件(0.3%)しかない。**タグが付くのは有名店だけ**なので、これ自体が知名度のフィルタとして
  働く。ただしOSMの`wikipedia`タグは記事の改名に追従しないことがあり
  (`ja:梅園 (台東区)`→現在は`梅園 (和菓子店)`)、`ja:`の接頭辞が抜けているものもある。

## データの出典・ライセンス表示

このリポジトリのCSVはtravel-log本体(MITライセンス)とは別物で、由来元の利用許諾条件が
そのまま適用される。特に次の2つは実データ(名称・座標・説明文)そのものが外部ソース由来のため、
再配布・二次利用時は出典表示が必要:

- **Wikipedia(ja)由来の説明文**: `tourist/spots.csv`・`goshuin/spots.csv`・
  `restaurant/spots.csv`の
  `description`列は、記事冒頭文を引用または要約したもの。[CC BY-SA 4.0](https://creativecommons.org/licenses/by-sa/4.0/deed.ja)
  (一部旧記事はGFDLとのデュアルライセンス)に基づき、利用時は出典(「出典: フリー百科事典
  『ウィキペディア(Wikipedia)』」+該当記事名・URL)の表示と、改変物を同一(または互換)
  ライセンスで提供することが求められる
- **Wikipedia/Wikidata由来の座標**: `tourist/spots.csv`の`lat`/`lng`は、Wikipedia記事の
  位置情報(9,282件、CC BY-SA 4.0)またはWikidata `P625`(1,501件、Wikidataのデータは
  [CC0](https://creativecommons.org/publicdomain/zero/1.0/))由来。以前はこのうち一部を
  OSM(osm_japan)で補完していたが、2026-07-23の見直しでその分は除外し、touristは
  Wikipedia(CC BY-SA 4.0)+Wikidata(CC0)由来のみになった(上記「各データの出典」参照)
- **OpenStreetMapのタグ・座標由来のデータ**:
  `goshuin/spots.csv`の座標・名称(Overpass API取得)・
  `suiyou_dodesho_overseas/spots.csv`の座標(Nominatim取得)・
  `restaurant/spots.csv`の座標(osm_japan取得)は
  [ODbL](https://opendatacommons.org/licenses/odbl/) (Open Database License)。まとまった量を
  再配布する場合は「© OpenStreetMap contributors」の表示と、データベース自体をODbL
  (または互換ライセンス)で提供することが求められる。
  **`restaurant`は座標が全件OSM由来**である点に注意(飲食店のjawiki記事はほとんど座標を
  持たないため。逆に、この理由でtouristには飲食店を入れていない — touristは2026-07-23に
  ODbL由来の座標を全件排除してWikipedia+Wikidata由来に統一してあり、そこへ飲食店を
  混ぜるとtourist側にODbLの表示義務が復活してしまう)。
  一部(55件)はjawiki記事側にも座標があり点検には使ったが、**値はOSMのものに統一している**
  (Wikipedia側の座標に誤りが2件あったため。上記「各データの出典」参照)
- `post_office/spots.csv`(国土数値情報)は非商用利用限定のデータセットである点に
  引き続き注意(上記「各データの出典」参照。CC BY-SA/ODbLとは別の制限)

`buratamori/spots.csv`・`suiyou_dodesho_domestic/spots.csv`は座標のみosm_japan(OSMミラー)で
1件ずつ裏取りしているが、説明文・立ち寄り順は一般知識からの作成であり、tourist/goshuinのような
Overpass APIでの一括抽出(データベースからの実質的な取り出し)ではなく既知の地点を個別に
参照した程度のため、上記のODbL表示義務は適用されないと考えている(まとまった量の再配布に
あたるかどうかの境界事例ではあるので、大量に追加抽出する場合は改めて要検討)。かつて
`tourist/spots.csv`と同内容をtravel-log本体の`db/init/tourist_spots.csv`に複製し出典表示なく
コミットしていたが、このリポジトリでの管理に一本化した。

## 将来構想(未実装)

`settings.json`の`series`は色・縁取り線の色・地図ピンの大きさ・ラベル(文字列/画像)のみに
対応している。シリーズの並び順以外のさらに複雑な表示条件(ズームレベルに応じた見た目の変化など)
が必要になった場合の形式は、現時点では未設計・未実装。
