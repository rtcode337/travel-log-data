# goshuin(御朱印)

- 全国の寺社仏閣 **51,950件**(御朱印を受けに行く先として使う)
- 出典: OpenStreetMap(Overpass API)の`amenity=place_of_worship`。ランク付けはWikipedia(ja)のページビュー
- ライセンス: **ODbL**(座標・名称)+ **CC BY-SA 4.0**(説明文)。再配布時は出典表示が必要(→[ライセンス](#ライセンス))

| ファイル | 内容 |
|---|---|
| `spots.csv` | スポットデータ(51,950件) |
| `settings.json` | スポット種別の設定(ランクの有効化・シリーズ2種とアイコン) |

`series`は **神社28,395 / 寺院23,555**(全件どちらか1つ)。**2026-08-11に、それまで
カテゴリだった神社・寺院をシリーズへ移した** —— 地図ピンの中身(アイコン)はシリーズが
決めるようになり、カテゴリは絞り込み専用になったため。移したあとカテゴリは空になっている。
アイコンは[観光地](../tourist/README.md)と同じもの(神社=鳥居 / 寺院=卍。地図記号から
描き起こしたパス)を使う —— 同じものを指すのに種別ごとに違う絵だと、重ねて表示したときに
別物に見えるため。判別はOSMの`religion`タグ(`shinto`/`buddhist`)なので、
記事の冒頭文から判定している観光地とは根拠が違う。

## 出典と生成

OpenStreetMap(Overpass API)の寺社仏閣(`amenity=place_of_worship`かつ
`religion=shinto`/`buddhist`)を唯一のスポット候補ソースとして機械生成した。かつてランク付き
(`goshuin_ranked.csv`)と未整理(`goshuin_unranked_z.csv`)の2ファイルに分かれていたものを、
1ファイルに統合して全件を作り直した(Wikipedia記事との突き合わせが甘く、人物記事や
宗教団体の記事にぶら下がった誤ランクが混ざっていたため)。

ランク(A〜E。CSVの`rank`列。**かつては`series`列だった**)は[tourist](../tourist/README.md)と同じく**Wikipedia(ja)の月次ページビュー数**
(bot除外)のパーセンタイル区分。**Wikipedia記事に紐付かない寺社は最下位のE**に置く。
御朱印は無名の寺社でも受けられるため、記事の無い寺社も落とさず同じファイルに収録している。

**この種別だけEの意味が違う。** 他の種別のEは「ページビューが下位20%」だが、
ここでは「そもそも記事が無く、ページビューで測れなかったもの」(44,955件=86%)。
記事のある6,995件は上から A5% / B15% / C30% / **D残り50%** に分ける
(かつては A5/B15/C30/D30/E20 の5段階だったが、Eを記事なしに明け渡したので
旧D・旧EをDにまとめた —— 境界を動かしていないので A・B・C の意味は他の種別と揃ったまま)。
**かつてはこれを`Z`というシリーズ値にして小さい灰色のピンで描いていた**。
ランクなし(白・Bと同じ大きさ)にすると4.4万件が地図を埋めるため、Eに入れている。

| ランク | 件数 | 内容 |
|---|---|---|
| A〜D | 6,995件 | Wikipedia記事と紐付き、ページビューでランク付けできたもの(A353 / B1,046 / C2,098 / D3,498) |
| E | 44,955件 | Wikipedia記事が無く、OSMに名前付きで採録されているだけのもの(旧`Z`) |

生成手順(スクリプトはリポジトリに含めていない一時作業):

1. Overpassで47都道府県の`admin_level=4`エリアごとにnode/way/relationを取得(境内が面で
   描かれている寺社を取りこぼさないため。way/relationは重心座標を採用)。`region`列は
   この絞り込みで確定するので、座標からの空間検索による近似判定は不要になった
2. 同名かつ200m以内の要素(建物way+POI nodeなど)を1件に統合
3. OSMの`wikipedia`タグ、無ければ`wikidata`タグ経由でja記事名を解決
4. その記事が**本当にその寺社の記事か**をWikipedia(ja)で検証し、
   外れたものはページビューで測れないものとして**E**にする:
   - 記事が実在する(リダイレクトは解決する)
   - 冒頭文が寺社の記事である(人物・宗教団体・自治体・駅などを除外。例:
     「崇徳天皇御廟」→崇徳天皇本人の記事、「立正佼成会」→宗教団体の記事)
   - 冒頭文に都道府県名が出る場合、OSM上の所在県と一致する(分社が総本社の記事を
     指しているケースを除外)
   - 「稲荷神社」のような社名全体を説明する総称記事にぶら下がっていない
   - 同じ記事を複数のスポットが指す場合、記事名と一致する1件だけを採用(曖昧なら全てE)
5. ページビューはWikimediaの月次ダンプ(`pageview_complete`、bot除外)由来の値を使い、
   記事のある寺社を A=上位5%/B=次15%/C=次30%/D=残り50% で区分(記事の無い寺社はE)。
   世界遺産の構成資産などページビューが伸びにくい寺社は目視で格上げする例外を許容する
   (touristと同じハイブリッド方式)
6. `description`はWikipedia記事の冒頭文(200字を目安に文単位で切り出し)、`name_kana`は
   冒頭文の読み仮名から機械抽出(記事名と名称が一致する場合のみ)。名称のうち英語併記
   (`猿田彦神社 (Sarutahiko Shrine)`など)は日本語表記だけに整形
7. A〜Dのスポットと100m以内で近接するE(同一寺社の別名タグ等、2,520件)は除外

## 出典記事のURL(`description`末尾)

`description`の末尾に、その文を取ってきたWikipedia記事を
`(出典: ja.wikipedia「北海道神宮」 https://ja.wikipedia.org/wiki/北海道神宮)`
の形で書いてある(6,960件。説明文のある6,994件のうち)。

**記事名はスポット名とは限らない。** 名称はOSM由来、記事はOSMの`wikipedia`/`wikidata`タグ
から解決したもので、`二荒山神社`(OSMの名前)の記事が`日光二荒山神社`だったり、
`高源寺`のように同名の記事が複数あって`高源寺 (行田市)`のように曖昧さ回避が付いていたりする。
生成時に記事名を残していなかったため、**説明文の冒頭とWikipedia記事の冒頭文を突き合わせて**
復元した(先頭18字の一致→記事名と説明文中の都道府県の一致→全文検索、の順)。

**残る34件はURLを付けていない。** 記事の冒頭文が書き換えられていて突き合わせできなかった行で、
誤った記事を指すよりは付けないほうがよい。付けるなら記事名を人手で確かめること。

URLの記事名は、**空白を`_`に、丸括弧を`%28`/`%29`に置き換える**。travel-log 側は
説明文中のURLを「空白か括弧が出たら終わり」で切ってリンクにするため
(`components/LinkedText.tsx`)、生の括弧を残すと行き先が欠ける。
日本語はそのまま置く(パーセントエンコードすると1文字9桁になり読めなくなる)。

## ライセンス

- **座標・名称**: OpenStreetMap(Overpass APIでの一括取得)由来で
  [ODbL](https://opendatacommons.org/licenses/odbl/)。まとまった量を再配布する場合は
  「© OpenStreetMap contributors」の表示と、データベース自体をODbL(または互換ライセンス)で
  提供することが求められる
- **説明文**(`description`列): Wikipedia記事の冒頭文由来で
  [CC BY-SA 4.0](https://creativecommons.org/licenses/by-sa/4.0/deed.ja)
  (出典表示と、改変物の同一・互換ライセンスでの提供が必要)
